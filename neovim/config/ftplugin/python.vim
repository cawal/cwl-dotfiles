setlocal tabstop=4 " number of spaces that a <tab> counts for, default 8
setlocal softtabstop=4 " number of spaces that a <tab> counts for while editing
setlocal expandtab " Create spaces instead of tabs
setlocal shiftwidth=4 " number of spaces in (auto) indent, if 0 uses tabstop

setlocal colorcolumn=80,81,82,83,84,85
